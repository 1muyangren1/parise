"""
测试脚本 - 检查环境和依赖是否正确安装
"""

import sys
import cv2
import numpy as np

print("=" * 50)
print("环境检查")
print("=" * 50)

# 1. Python 版本
print(f"Python 版本: {sys.version}")

# 2. OpenCV 版本
print(f"OpenCV 版本: {cv2.__version__}")

# 3. NumPy 版本
print(f"NumPy 版本: {np.__version__}")

# 4. 检查摄像头（可选）
print("\n检查摄像头...")
cap = cv2.VideoCapture(0)
if cap.isOpened():
    print("✓ 摄像头可用")
    cap.release()
else:
    print("✗ 摄像头不可用（如果没有摄像头这是正常的）")

# 5. 检查 torch（可选）
try:
    import torch
    print(f"✓ PyTorch 版本: {torch.__version__}")
except ImportError:
    print("✗ PyTorch 未安装（可选依赖）")

# 6. 检查 ultralytics（可选）
try:
    from ultralytics import YOLO
    print("✓ Ultralytics YOLO 已安装")
except ImportError:
    print("✗ Ultralytics 未安装（可选依赖）")

print("\n" + "=" * 50)
print("环境检查完成！")
print("=" * 50)